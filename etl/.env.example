# Dagster ETL environment (optional — aml_platform/docker-compose.yml loads
# this file if present; copy to etl/.env and adjust as needed).
# Required variables (POSTGRES_URI, DAGSTER_HOME) are provided by
# docker-compose.yml; anything here is additive.

# Example additional Dagster configuration:
# DAGSTER_POSTGRES_USER=postgres
# DAGSTER_CURRENT_IMAGE=dagster_etl
